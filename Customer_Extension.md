Search for the application name – MIGRATE YOUR DATA

Select the application on the left

Go to Create and then ‘Create using staging tables’

Give your project a name and select ‘Local S/4HANA Database Schema

Go to step 2 in the bottom.

Type – Customer, select object – ‘Customer-extend existing record by new org levels’

Open project and select the right arrow.

Inside the project, let the running activities complete.

Select ‘Mapping tasks’ and choose upload file

Select ‘upload’

Select the file

Inside the file, select the ‘General Data’ file and write the customer number, assign a run-key

On the ‘Sales data’ file, fill the required data

Fill the other information mentioned below.

Save the file.

Upload the file.

Come on the previous screen, select mapping data and select prepare.

Check on consistency check and select ‘Prepare staging tables’

The activity starts to run. You can view the same on running activities app.

The status has changed.

Select ‘Mapping tasks’ and click simulate button.

Go to Mapping tasks, select the activities to confirm and press confirm.

If the simulation was successful with no errors, Now press the ‘Migrate’ button.

Go to ‘Monitoring’ app and check the status.

Output – the data has been successfully migrated.